<% if @error_message %>
	<ul>
		<li><%= @error_message %></li>
	</ul>
<% end %>

<% if @cart_items %>
	<% @cart_items.each do |cart_item| %>
		<div id="cart-item-<%= cart_item.id %>">
			<%= render 'users/check_outs/cart-item', cart_item: cart_item %>
		</div>
	<% end %>
<% end %>